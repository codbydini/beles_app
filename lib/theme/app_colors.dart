import 'package:flutter/material.dart';

class AppColors {
  // Primary dark blue color extracted from the BELES HOLDING logo
  static const Color primaryBlue = Color(0xFF1F4E79); 
  static const Color secondaryBlue = Color(0xFF2B6B9B);
  
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;
  
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  
  static const Color accent = Color(0xFF00C853); // Green for positive actions (money received)
  static const Color error = Color(0xFFD32F2F);  // Red for negative actions
}
